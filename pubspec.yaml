name: vim_math3d
description: Math3D is an efficient low-level math library forked from System.Numerics and MonoGame with additional functionality and a strict struct layout for cross platform serialization.
version: 1.6.1
homepage: 'https://www.vimaec.com/'

environment:
  sdk: '>=3.2.4 <4.0.0'

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^3.0.1

# For information on the generic Dart part of this file, see the
# following page: https://dart.dev/tools/pub/pubspec